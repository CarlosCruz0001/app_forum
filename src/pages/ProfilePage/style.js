import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginTop: 40,
    alignItems: "center",
    backgroundColor: "#afaf",
    height: '99%',
  },
  info: {
    marginTop: 10,
    fontSize: 24,
    backgroundColor: '#bbb',
  },
  bio: {
    fontSize: 16,
  },
});
